# APPAUTO 测试用例仓库

此仓库用于存储和版本管理 APPAUTO 平台的测试用例。

## 目录结构

```
testcase-repo/
├── testcases/              # 测试用例目录
│   └── {应用名}/
│       └── {分类名}/
│           └── {用例名}.json
├── metadata/              # 元数据
│   └── index.json
└── README.md
```

## 自动生成

此仓库由 APPAUTO 系统自动管理，请勿手动修改。
